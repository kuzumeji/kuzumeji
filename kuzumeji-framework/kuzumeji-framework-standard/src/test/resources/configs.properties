# usergui.properties

include = colors.properties
include = sizes.properties

key = This \n string \t contains \, escaped \\ characters
vars=foo,bar,baz

MESSAGE_BASENAME = messages
ERROR_MESSAGE_BASENAME = error-${MESSAGE_BASENAME}
